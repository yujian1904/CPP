#include "iostream"
//#include "vector"
//#include "algorithm"

using namespace std;

int main() {
    cout << "hello" << endl;
    /*
    vector<int> vi;
    vi.push_back(4);
    vi.push_back(5);
    vi.push_back(7);
    vi.push_back(8);
    cout << vi.size() << endl;
    vi.insert(vi.begin() + 2, 18);
    cout << vi.at(2) << endl;
    cout << "_______________________________________________________" << endl;
    for (int i = 0; i < vi.size(); ++i) {
        cout << vi[i] << endl;
    }
    //vector<int>::iterator it = find(vi.begin(), vi.end(), 7);
    vi.erase(std::find(vi.begin(), vi.end(), 5));
    cout << "_______________________________________________________" << endl;
    for (int i = 0; i < vi.size(); ++i) {
        cout << vi[i] << endl;
    }
    */
    return 0;
}